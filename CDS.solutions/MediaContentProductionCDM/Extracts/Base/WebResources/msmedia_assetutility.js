(function (AssetMedia) {
    var Utility = {};
    AssetMedia.Utility = Utility;
    Utility.displayIconInView = function (a, b, c) {
        return [JSON.parse(a).msmedia_logo_url_Value];
    }
}(window.AssetMedia = window.AssetMedia || {}))